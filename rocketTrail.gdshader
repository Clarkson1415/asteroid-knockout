shader_type particles;

uniform sampler2D tex : source_color; // your particle texture
uniform vec4 start_color : source_color = vec4(1,1,1,1);
uniform vec4 end_color   : source_color = vec4(1,1,1,0);

void process() {

}
